#include<stdio.h>//by passing it as parameter we can manipulate the data inside it
 void fun(int *a,int n){
     a[0]=15;
 }
int main()
{
    int a[5]= {2,4,5,6,7};;
    int n=5;
    fun(a,n);
    for(int i=0;i<5;i++)
    {
        printf("%d",a[i]);
    }
}